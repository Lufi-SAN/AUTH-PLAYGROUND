import type {
  Request,
  Response,
  NextFunction,
} from '../../../types/user.types.js'

export async function verifyEmail(
  req: Request,
  res: Response,
  next: NextFunction,
) {
  // try {
  // } catch (error) {
  //     return next(error)
  // }
}
